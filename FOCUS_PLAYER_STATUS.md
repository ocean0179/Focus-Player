# Focus Player

## 1. 프로젝트 개요

**Focus Player**는 노트북을 사용하면서도 YouTube, Shorts, 추천 영상 등 집중을 방해하는 요소에 노출되지 않고, 음악과 필요한 공부 도구만 사용할 수 있도록 만드는 개인 집중 환경이다.

핵심 목표는 사용자의 의지에 의존해 유혹을 참게 만드는 것이 아니라, **유혹으로 이어지는 접근 경로 자체를 줄이는 것**이다.

---

## 2. 최종 목표

Focus Player 실행만으로 아래 흐름이 완성되는 것을 목표로 한다.

```text
Focus Player 실행
        ↓
집중 세션 시작
        ↓
타이머 + 음악
        ↓
필요한 공부 도구만 사용
        ↓
YouTube / Shorts / 추천 피드 노출 최소화
        ↓
집중 기록 자동 저장
        ↓
휴식
        ↓
다음 세션
```

최종적으로는 웹 프로토타입 검증 후 데스크톱 앱으로 패키징하여,

```text
바탕화면 아이콘 클릭
→ Focus Player 실행
→ 바로 집중 시작
```

이 가능한 상태를 목표로 한다.

---

## 3. 현재 진행도

- **핵심 기능 기준:** 약 80~85%
- **최종 제품 기준:** 약 55~60%

현재는 기술 검증 단계는 대부분 통과했으며, 앞으로는 실사용 UX와 확장 기능을 다듬는 단계이다.

---

## 4. 현재 구현된 기능

### 4.1 집중 타이머

- 50분 집중 / 10분 휴식 기본 설정
- 시작
- 일시정지
- 계속
- 종료
- FOCUS ↔ BREAK 자동 전환
- 현재 세션 번호 표시
- 타이머 상태 관리
  - `IDLE`
  - `RUNNING`
  - `PAUSED`

---

### 4.2 집중 기록

- 오늘 누적 집중시간 표시
- 완료된 집중 세션 수 기록
- `localStorage` 기반 저장
- 다음 상황에서 기록 저장
  - 일시정지
  - 종료
  - 세션 자연 완료
  - 페이지/창 종료
- 집중 중에는 메모리에서 초 단위로 누적
- 의미 있는 상태 변경 시 영구 저장

---

### 4.3 YouTube 음악 플레이어

YouTube 홈페이지를 직접 열지 않고 **YouTube IFrame Player API**를 사용하여 앱 내부에서 음악을 재생한다.

현재 구현:

- 영상 재생
- 재생 / 일시정지 토글
- 이전 곡
- 다음 곡
- 현재 곡 제목 표시
- 플레이어 상태와 버튼 상태 동기화
- Live Server 기반 실행
- YouTube 오류 153 해결

---

### 4.4 플레이리스트

- `data/playlists.js`에서 플레이리스트 데이터 별도 관리
- 여러 플레이리스트 등록 가능
- 플레이리스트 버튼으로 목록 변경
- 영상 ID 배열 기반 재생
- 현재 곡 제목 자동 변경
- 마지막 선택 플레이리스트 저장
- 새로고침 후 마지막 플레이리스트 복원 기반 구현

플레이리스트 데이터 예시:

```js
export const PLAYLISTS = {
  default: {
    name: "Default",
    tracks: [
      {
        videoId: "VIDEO_ID",
        title: "Track Title",
      },
    ],
  },
};
```

주의:

```text
videoId
```

속성명은 대소문자를 정확히 유지한다.

---

### 4.5 버튼 및 상태 UI

- 버튼 Hover 피드백
- 클릭 시 눌림 효과
- 비활성 상태 표시
- 타이머 실행 시 시작 버튼 비활성화
- 일시정지 시 시작 버튼을 `계속`으로 변경
- 음악 재생 상태에 따라
  - `재생`
  - `일시정지`
  로 버튼 문구 동기화

현재 디자인은 임시이며 최종 디자인은 기능 완성 후 리워크할 예정이다.

---

## 5. 현재 프로젝트 구조

```text
focus-player/
├─ index.html
├─ README.md
│
├─ css/
│  ├─ main.css
│  └─ components.css
│
├─ js/
│  ├─ app.js
│  ├─ timer.js
│  ├─ player.js
│  ├─ session.js
│  ├─ storage.js
│  ├─ ui.js
│  │
│  └─ config/
│     ├─ constants.js
│     └─ defaults.js
│
├─ data/
│  └─ playlists.js
│
└─ assets/
   ├─ icons/
   └─ images/
```

---

## 6. 각 파일 역할

### `index.html`

전체 화면의 HTML 구조를 담당한다.

### `css/main.css`

- 전체 레이아웃
- 기본 배경
- 타이머 배치
- 음악 영역 배치

### `css/components.css`

- 버튼
- 플레이어 박스
- 세션 정보
- 각 UI 컴포넌트 스타일

### `js/app.js`

각 모듈을 연결하는 애플리케이션의 중심 파일이다.

직접 모든 기능을 수행하는 것이 아니라,

```text
Timer
Session
Player
Storage
UI
```

를 연결하고 이벤트 흐름을 관리한다.

### `js/timer.js`

순수 타이머 기능 담당.

- 시작
- 일시정지
- 초기화
- 완료
- 남은 시간
- 타이머 상태

### `js/session.js`

집중/휴식 세션 관리.

- 현재 세션 종류
- 세션 번호
- 완료된 집중 세션
- FOCUS ↔ BREAK 전환

### `js/player.js`

YouTube IFrame Player API 담당.

- 초기화
- 재생
- 일시정지
- 재생/일시정지 토글
- 플레이리스트 로드
- 이전/다음 곡
- 볼륨
- 현재 플레이어 상태

### `js/storage.js`

`localStorage` 관련 로직 담당.

- 설정 저장/불러오기
- 집중 기록 저장/불러오기
- 마지막 플레이리스트 저장/불러오기

### `js/ui.js`

화면 출력과 DOM 요소 관리.

- 타이머 표시
- 세션 상태 표시
- 누적 집중시간 표시
- 현재 곡 제목
- 버튼 상태
- 시간 포맷 변환

### `js/config/constants.js`

프로그램 내부에서 의미가 고정된 값 관리.

예:

```js
SESSION_TYPE
TIMER_STATE
STORAGE_KEY
```

### `js/config/defaults.js`

사용자가 향후 변경할 수 있는 설정의 초기값 관리.

예:

```js
focusDuration
breakDuration
volume
autoStartBreak
autoStartFocus
```

### `data/playlists.js`

음악 및 플레이리스트 데이터 관리.

---

## 7. 개발 원칙

### 역할 분리

각 파일은 자신의 책임만 가진다.

예:

```text
timer.js
→ 시간만 관리

session.js
→ 세션 의미만 관리

ui.js
→ 화면만 관리

storage.js
→ 저장만 관리

app.js
→ 각 기능을 연결
```

---

### 상태와 UI 분리

화면 표시 자체가 애플리케이션 상태를 결정하지 않는다.

```text
실제 상태 변경
→ UI가 상태를 읽음
→ 화면 갱신
```

방향을 유지한다.

---

### 저장 전략

실시간으로 바뀌는 모든 값을 매초 영구 저장하지 않는다.

```text
집중 중
→ 메모리 값 갱신

일시정지 / 종료 / 완료 / 창 종료
→ localStorage 저장
```

방식을 기본으로 한다.

---

### 기능 우선, 디자인 후순위

현재 단계에서는:

```text
기능 구현
→ 실제 사용
→ 문제 확인
→ 기능 수정
→ 핵심 기능 완성
→ 최종 디자인
```

순서로 진행한다.

---

## 8. 앞으로 구현할 기능

### Priority 1 — 현재 플레이리스트 선택 상태 표시

- 현재 선택된 플레이리스트 버튼 강조
- 새로고침 후에도 선택 상태 표시

---

### Priority 2 — 볼륨 조절

- 볼륨 슬라이더
- YouTube Player와 실시간 연동
- 마지막 볼륨 저장
- 새로고침 시 복원

---

### Priority 3 — 세션 완료 UX

- 집중 완료 안내
- 휴식 시작 안내
- 휴식 완료 안내
- 필요 시 간단한 알림음
- 세션 전환 시 버튼 상태 초기화

---

### Priority 4 — 설정 UI

사용자가 직접 변경 가능하도록 구현.

- 집중시간
- 휴식시간
- 기본 볼륨
- 자동 휴식 시작
- 자동 집중 시작

---

### Priority 5 — 공부 보조 기능

실제 사용 과정에서 Focus Player 밖으로 나가게 만드는 이유를 분석한 뒤 필요한 기능을 내부로 흡수한다.

후보:

- 사전
- 단순 검색
- 공부용 도구 바로가기

무조건 추가하지 않고 실제 사용 필요성을 확인한 뒤 구현한다.

---

### Priority 6 — 기록 및 통계

- 날짜별 집중시간
- 세션 로그
- 일간 통계
- 주간 통계
- 집중시간 변화

---

### Priority 7 — 디자인 리워크

핵심 기능 안정화 이후 진행.

- 전체 레이아웃
- 버튼 디자인
- 타이머 비주얼
- 플레이리스트 UI
- 플레이어 크기
- 반응형 레이아웃
- 상태별 시각적 구분

---

### Priority 8 — 데스크톱 앱화

웹 버전 실사용 검증 이후 진행.

후보:

- Electron
- Tauri

목표:

```text
Focus Player 아이콘 클릭
→ 앱 실행
→ 즉시 집중 환경 진입
```

---

## 9. 현재 확인된 버그 및 해결 내역

### YouTube 오류 153

**원인:** `file://` 방식으로 실행하여 정상적인 Referrer가 존재하지 않음.

**해결:** Live Server를 사용하여 `http://localhost...` 방식으로 실행.

---

### 종료 버튼 연타 시 집중시간 증가

**원인:** `timer.reset()` 과정에서도 `onTick()`이 호출되면서 집중시간이 증가함.

**해결:** 실제 집중시간 누적 조건에 `TIMER_STATE.RUNNING` 확인 추가.

---

### `this.formatTime is not a function`

**원인:** `UI` 클래스에서 `formatTime()` 메서드가 누락됨.

**해결:** `formatTime()` 메서드 추가.

---

### `TIMER_STATE has already been declared`

**원인:** `TIMER_STATE` 중복 import/선언.

**해결:** `constants.js`의 `TIMER_STATE`만 import하도록 통일.

---

### 플레이리스트 변경 시 영상이 바뀌지 않음

**원인:** 플레이리스트 데이터에서 `videoID`를 사용했지만 코드에서는 `videoId`를 참조함.

JavaScript는 대소문자를 구분하므로:

```text
videoID != videoId
```

**해결:** 모든 플레이리스트 데이터의 속성을 `videoId`로 통일.

---

## 10. 현재 개발 단계

현재 Focus Player는 **실사용 가능한 프로토타입** 상태이다.

핵심 기술 검증:

```text
타이머           완료
세션 관리        완료
집중 기록        완료
YouTube 재생     완료
플레이리스트     완료
상태 관리        완료
기본 저장        완료
```

앞으로는 기술적 가능성 확인보다 **실제 사용 경험을 개선하고 기능을 확장하는 작업**이 중심이 된다.

---

## 11. 변경 이력

### 2026-08-27

- Focus Player 프로젝트 현황 문서 최초 작성
- 현재 구현 기능 정리
- 프로젝트 구조 정리
- 개발 원칙 정리
- 향후 구현 로드맵 정리
- 기존 버그 및 해결 내역 정리

---

## 12. 다음 작업

**현재 선택된 플레이리스트를 UI에서 명확하게 표시하는 기능 구현**

이후:

```text
선택 플레이리스트 표시
→ 볼륨 조절 및 저장
→ 세션 완료 UX
→ 설정 UI
```

순서로 진행한다.
